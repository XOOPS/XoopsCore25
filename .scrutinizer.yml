build:
  nodes:
    analysis:
      tests:
        override:
          - php-scrutinizer-run

filter:
  excluded_paths:
    - '_archive/'
    - 'tests/'
    - 'tests_codex2/'
    - 'extras/'
    - 'docs/'
    - 'htdocs/xoops_lib/vendor/'
    - 'htdocs/install/'

tools:
  php_analyzer: true
